ReferenceManager = (function(name){

    ExtendedDelta = (function(){
        Items = {};
        Model = {}; //// Loopback model created from CSL file.

        function getDataType(FIELD){

        }

        function setValue(canvas, value){
            
        }

        function applyDelta (delta){
            if (getDataType(FIELD)=="plaint-text"){
                setVal();
            } else if (getDataType(FIELD) == "text"){
                Shadow.applyDeltas(canvas, delta);
            }
        }

        function set(){

        }
        function 

        return {};
    })();

    

    return {};
});

Sources = ReferenceManager("Source");
Credits = ReferenceManager("Credit");
Exhibits = ReferenceManager("Exhibit");