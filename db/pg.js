/*const { Pool } = require('pg')

const connection = require("./secrets.json");
const pool = new Pool(connection);


module.exports = {
  query: (text, params, callback) => {
    return pool.query(text, params, callback)
  }
}
*/